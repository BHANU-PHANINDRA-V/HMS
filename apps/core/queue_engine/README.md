# Queue Engine

Reserved for queue orchestration that may combine appointments, tokens, sessions, linked lists, and priority queues.

Keep engine coordination here and persist domain data through the relevant apps, especially `appointments`, `queues`, and `sessions`.
