# Core

Framework-independent core building blocks live here.

This is the right place for algorithm implementations and internal engines that support multiple apps. For this project, linked list and priority queue implementations belong in `algorithms/`, while queue orchestration belongs in `queue_engine/`.
