# Static

Public static assets served by Django or a web server.

Use this folder for CSS, JavaScript, images, icons, fonts, and vendor files that are part of the application codebase. Uploaded user files do not belong here; use `media/` for uploads.
