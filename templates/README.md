# Templates

Django template folders grouped by role and feature.

No HTML pages are implemented yet. Future templates should be placed under the folder that matches their role or feature, such as `patient/`, `doctor/`, `staff/`, `appointments/`, or `payments/`.
