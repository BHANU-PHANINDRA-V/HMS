# Requirements

Dependency files are split by environment.

- `base.txt`: shared dependencies.
- `development.txt`: local development dependencies.
- `production.txt`: production-only dependencies.
- `testing.txt`: test-only dependencies.

These files are placeholders until dependency selection begins.
