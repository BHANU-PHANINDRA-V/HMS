# Tests

Project-level tests live here when they are not owned by a specific Django app.

Prefer app-local tests for domain behavior. Use this folder for integration tests, cross-app workflows, and system-level test utilities.
