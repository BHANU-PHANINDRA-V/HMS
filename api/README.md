# API

Versioned API boundary for future REST endpoints.

Keep API routing, serializers, and API views grouped by version. Domain behavior should still be delegated to app services instead of being implemented directly in API views.
