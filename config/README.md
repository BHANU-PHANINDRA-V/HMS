# Configuration

Project-level Django configuration lives here.

- `settings/`: environment-specific Django settings.
- `routes/`: top-level web and API route composition.
- `security/`: future security configuration notes or policy helpers.
- `asgi.py` and `wsgi.py`: deployment entry points when implemented.
